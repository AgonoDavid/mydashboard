import React from 'react';
import { Flex, Text, Box, Divider, Container, Wrap } from '@chakra-ui/react';
import { Navbar } from './Navbar';

export const Mainboard = () => {
  return (
    <Box w={'80%'}>
      <Navbar />
    </Box>
  );
};
